# XGraph web

_Browser application boundary_

---

The frontend will contain task controls, account tables, exports, and a bounded graph view. Runtime code will call the XGraph API only; it will not import or depend on the upstream `twscrape/` package. Framework files will be added when the first usable page is implemented.
